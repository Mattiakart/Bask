# Canonical origin. Used for the canonical link, Open Graph tags, robots.txt
# and the sitemap. No trailing slash needed — it is stripped either way.
NEXT_PUBLIC_SITE_URL=https://baskinwardrobe.example
